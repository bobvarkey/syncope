import Foundation

enum Constants {
    static let appName = "MedCalc"
    static let disclaimer =
        "This app is for informational and educational purposes only and does not provide medical diagnosis, treatment, or emergency advice."

    enum Storage {
        static let sidebarGroups = "medcalc.sidebar.openGroups.v1"
        static let hasOnboarded = "medcalc.hasOnboarded.v1"
        static let colorSchemePreference = "medcalc.colorScheme.v1"
    }
}
